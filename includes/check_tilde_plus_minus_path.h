#ifndef CHECK_TILDE_PLUS_MINUS_PATH_H
# define CHECK_TILDE_PLUS_MINUS_PATH_H

char	*check_tilde_plus_path(t_inst *inst);
char	*check_tilde_minus_path(t_inst *inst);
int		check_arg(t_inst *inst, const char *str);

#endif
